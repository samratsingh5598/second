### Most used github commands

1. git status -> check status of current directory
2. git add <filename> -> add specific file for commit
3. git add . -> add all files for commit
4. git commit -> commit the changes
5. git push -> puh the changes to github
